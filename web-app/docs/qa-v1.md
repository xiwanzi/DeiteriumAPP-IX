# 网页第一版验收

日期：2026-09-08。对象：`web-app` 0.1.0，本机 Chrome 与 Node.js v24.14.1。没有测试或修改生产账号、游戏经济、真实后台服务。

| 检查 | 结果 |
| --- | --- |
| Vite 生产构建 | 通过；JS 277.78 kB / gzip 87.10 kB，CSS 53.01 kB / gzip 11.49 kB（不含媒体） |
| Node 领域测试 | 15 项通过，0 失败 |
| 页面与宽度 | 7 个页面 × 1440 / 768 / 390 / 360，共 28 个组合；无整页横向溢出、无缺失图片 |
| 深色模式 | 7 个页面逐页检查，刷新保持主题；截图已目视检查代表页面 |
| 完整交互流程 | 26 项通过，流程无 JavaScript 运行异常 |
| 静态构建服务 | `/market` 路由直接访问成功，`/` 搜索快捷键可用，资源类型正确，缺失文件 404，非 GET/HEAD 请求 405，`/api/*` 为 503 `DEMO_ONLY` |
| 素材与引用 | 复用指定任务的现成文件与 App 素材；未使用生成请求返回的图片 |

## 26 项浏览器流程

购物袋加购、创建订单、刷新保留订单、全局搜索、Escape 关闭、市场分类、再次点击取消分类、市场发布、委托接取、接取后从大厅移除、我的接取列表、精确转账、公共消息发送、引用回复、AI 示例标注、资料保存、官方商品发布、封禁记录、被封账号禁止进入、解封保留历史、案件意见保存、浏览器返回、手机导航收起、手机弹窗边界、减少动态效果、运行异常检查。

## 检查中修正

- 统一表单显式标签，避免下拉选项或文本域默认内容进入标签名称。
- 调整商城推荐图片与标题位置，复用服务器实景并通过 CSS 裁切原图黑边。
- 修正 React 确认框中 key 的传递方式。
- 收藏按账号保存，私聊使用双方稳定标识，幂等按账号/动作作用域并检查请求内容。
- 账单日期筛选使用与页面时间相同的本地日历日，避免 UTC 日期与显示日期不一致。

## 证据与限制

浏览器脚本：`output/playwright/layout-check.cjs`、`flow-check.cjs`、`production-check.cjs`。截图包含 `store-1440.png`、`market-1440.png`、`commissions-390.png`、`information-dark.png`、`admin-390.png`、`production-home.png` 等。

截图在页面进入动画结束后拍摄；没有把过渡中的半透明画面作为最终效果。

以上为桌面 Chrome 下模拟不同视口，不等于 iPhone Safari 或 Android 真机验收。生产 Cookie、CORS、WebSocket、Web Push、真实 API、跨设备同步、游戏邮箱、服务端自动结算和交易接管仍待联调。页面功能边界见 [README](../README.md)。
