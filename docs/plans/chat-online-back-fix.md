# 公共聊天在线玩家与连续返回修复（2026-09-26）

基于最新 `origin/main` `cbc522d14ba565db8068cff97b2b31daa8e00e91`。Android 树 `4fbc9624866b84d89bcce9d7bfd5be2e46d455fc` 与已发布 2.0.13（21300）一致；本次只读查询公网更新接口也返回最新 21300。

## 已确认范围

- 在线玩家指当前游戏服务器在线玩家，沿用 `/chat/presence`、`/chat/online-players` 既有契约；不能以 App 联系人缓存或历史目录当在线名单。
- Go 从已鉴权、启用聊天的 Core 节点实时快照读取，按游戏 UUID 去重；节点断开立即去除，未收到快照与已确认无人在线分别表示不可用和空列表。
- 名单查询只读，不创建账号或改变白名单。沿用已有 UUID → playerRef 映射，屏蔽已注销账号；没有 App 账号的在线玩家可列出、提及，不提供私聊按钮。
- App 弹窗打开及前台显示期间刷新，提供加载、空态、失败重试和滚动。服务不可用不伪装成零人在线。
- 有页面可返回时保持系统返回回调注册；IME 优先收起，页面收到返回时读取当前窗口 IME 可见性，避免动画高度造成回调禁用窗口。无固定延时和双击退出逻辑。
- 不改变付款、主题、玻璃或其他业务；版本仍为 21300，本轮不推送、不发布。

## 验证计划

后端验证鉴权、快照去重、空态、节点断连、未注册及已注销玩家；Android 验证真实请求结果替换缓存、失败重试、长列表和连续返回。运行相关测试、构建及 lint。模拟器证据与用户设备手势验收分别记录。
