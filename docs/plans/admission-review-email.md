# 白名单审核邮件接入

2026-09-13。用户已确认通过 v5、拒绝 v3 的版式和文案，授权接入后台邮件管理、使用已有 SMTP 配置并完成部署。用户明确收件人为申请中填写的 `QQ号码@qq.com`。

- 审批通过/拒绝与邮件任务在同一数据库事务提交，一份申请的最终审核只生成一个任务。保存玩家名、收件地址、拒绝原因与模板版本；重试不重新读取其他玩家的地址，不发送审核人私有备注。
- 复用现有邮件队列和发送工作器，兼容 MariaDB 10.5，保留原管理提醒与连接测试。模板提供 UTF-8 纯文本/HTML、CID 徽章、HTML 转义及固定 Message-ID。
- 邮件管理增加白名单审核提醒开关、两种模板预览/测试、类型与收件人展示。测试发到已配置的管理员测试收件列表，模板明确标记测试；正式邮件发给申请人。
- 关闭白名单提醒时的新审核记录为已跳过；已经排队的该类邮件暂停发送。邮件总开关关闭时暂停所有投递。重新开启不自动补发历史已跳过记录。
- 发送前复核申请和资格：已撤销/已被后续申请取代的旧通知取消，保留记录。邮件失败不撤销审核决定；沿原任务重试。
- 手动添加、移除、旧名单导入不冒充申请审批，不发这两类结果邮件。当前 70 条撤销记录不得恢复，也不为上线前的审批补发。
- 申请页说明审核结果会发送到所填 QQ 的邮箱。App 和游戏插件保持当前版本。

验收包括审批与入队的原子性、重复和并发、收件隔离、真实拒绝原因转义、开关/跳过/暂停、旧通知取消、SMTP 接收后的不重复领取，浏览器表单/模板预览及实际已授权测试邮箱投递。线上正式申请不用于破坏性测试。
