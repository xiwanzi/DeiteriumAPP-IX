# 2.0.2 支付并行验收

2026-09-08。App 2.0.2 (20200)，用户明确要求识别时直接提交付款。

共用 PaymentExperience 在进入模拟识别时启动原付款回调，扫描计时独立进行，删除原 470ms/100ms 等待。识别结束后才展示真实结果；早到结果立即保存在可恢复状态中。转账在确认按钮固定请求 ID 与已验证收款人，重试保持同键，原待确认结果须匹配键/收款人/金额/备注。没有修改后端资金接口。

本次构建命令：gradlew.bat :ui-lab:assembleDebug :ui-lab:assembleDebugAndroidTest :ui-lab:testDebugUnitTest :ui-lab:lintDebug --offline --console=plain。

- 113 项单元测试通过，0 failures/errors；Lint 0 errors / 29 warnings。
- Android 15 模拟器、独立 androidTest 回调：fast / slow / failure / recovered 四项均通过。
- 确认回调在最短识别时间前已开始；快成功仍等待识别、页面移除后恢复不重复回调；慢请求（含关闭动效）继续等待；失败不显示成功；恢复成功不被晚到失败覆盖。
- 测试回调不调用实际金融 API，未为时序验收发起真实转账或订单。
- 包名/签名沿用原应用；发布证据见 2.0.2 部署记录。模拟器不替代用户手机的网络和手感测量。
