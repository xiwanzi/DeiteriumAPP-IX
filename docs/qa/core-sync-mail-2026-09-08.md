# Core / Sync / 独立 Mail 验证记录

2026-09-08，`backend-rewrite` 工作线。隔离 Youer `1.21.1-f15a736d`、NeoForge `21.1.248`、Java 21，回环地址；专用 Core/Sync 数据库与独立 Mail 数据库。无生产玩家或生产经济数据。最终交付文件的摘要另列于交付目录。

## 数据库验证

SyncStore 的 9 项真实 MariaDB 测试全部通过，未跳过：双节点竞争唯一会话、旧 epoch 禁止覆盖、完整加载前拒绝领取、LEASED 到期仍隔离、玩家数据/背包/回执一起回滚、已提交回执重启可查、精确 epoch 人工恢复审计、错误库存域拒绝、持有领取行锁时心跳不阻塞与不延长未知领取。

Core 的 RPC 过期重放新增 4 项测试：原已提交转账回执保持 COMPLETED，重复资金执行次数为一；原 UNKNOWN 不重新取得执行权；新过期操作不建执行日志；已完成邮件过期重放不再调用 Mail。不同内容同 ID 仍拒绝，原回执不被覆盖。

## 实际服务端验证

使用真实 MOD JAR：SophisticatedBackpacks `3.25.78.2107`、SophisticatedCore `1.4.90.2299`。测试客户端是仅连接回环的协议夹具，会协商对应频道并跳过无关渲染数据，不是完整 MOD 客户端，不能代表界面或真机手感验收。

| 项目 | 结果与证据 |
| --- | --- |
| 原 .2 补丁兼容 | .3 正常加载，保留服务端本地 NBT/游戏模式修复；没有依靠定时等待伪造 READY |
| 玩家物品快照 | 附魔/耐久/名称/Lore/PDC 钻石剑、嵌套潜影盒往返一致；实际 Sync 提交并读取保存回执 |
| 背包外部存储 | 真实精妙背包的 7 个钻石内容进入 backpack_data；清空后覆盖旧内容；修复 MOD 合并旧键行为与缓存刷新 |
| 物品库别名保护 | 已初始化外部 UUID 背包入库被明确拒绝；不会克隆同一外部库存引用 |
| 损坏 NBT 加载 | 注入无效压缩数据后加入被拒绝；失败内存不覆盖 SQL，phase=OFFLINE；测试后恢复了原测试快照 |
| 保存 SQL 失败 | 对 player_data 注入 INSERT 失败，旧已提交玩家数据未变，0 份虚假回执，FAILED 隔离并踢出 |
| 提交成功但响应丢失 | JDBC 真实 commit 后注入响应异常，仍保留唯一保存证明，FAILED 隔离；控制台按原操作读回同证明 |
| 历史恢复 | 物品回到历史快照；当前本服 CREATIVE 与飞行能力保留；缺失玩家历史不返回成功，缺失外部背包历史拒绝部分恢复 |
| 独立 Mail 正常领取 | Core 冻结快照创建邮件，玩家真实 `/dmail claim` 恰好多 3 个绿宝石；同创建请求复用同 mailId，二次领取不增物，CLAIMED 后撤回返回 ALREADY_CLAIMED |

13:05:29 正常 Mail 链路：`probe_delivery_d158f78b-8b05-4735-a756-23205d834d5d`，mailId `1`，最终 CLAIMED。13:05:39 提交响应丢失测试：操作 `b2f039e5-6e39-41d1-b2eb-95904136b12b`，保存证明 `youermodsync-v1:b2f039e5-6e39-41d1-b2eb-95904136b12b:7252670e-f47b-403a-98ee-dfe5dccc43a8:6a0c9dc8d668549704d7cffa1eeaa0a6787d71b13e6d78f7996a835259d31552`。

原始测试结果保存在本机隔离目录的 `plugins/DeuteriumCoreProbe/*.json`。探针源码在 `deuterium-core/integration-probe`，不得安装生产；它包含仅对专用测试库可用的故障注入。

## 验证范围与后续批次

上表是本日开发过程中的实际服务端验证。后续同批 Mail 保存证明恢复、最终 JAR 回归、生产三常驻服与真实 MOD 客户端验收分别追加结果，不把早期成功移作尚未执行的最终验证。生产更新由部署任务统一停服、更换、备份与核对；此记录不是生产上线声明。

## 13:35 最终配套包回归

Core `388534536baa87afa04e08e0155edb7cfe2f53d9e358497ac694cfecacb9fd43`、Sync .3 `376411646a903bcdb53808b68d94673f9a7f5ea9e47fde4e15957395517a9151`、独立 Mail .6.1 `9c8b4585799812e7874094d023e057acd059f9a0716210ac1b476ca009249cfa`。Core 29 项测试、Sync 9 项真实 MariaDB 测试全部通过，0 失败、0 跳过。

最终组合正常启用，受控重载限制生效。再次执行真实精妙背包内容保存/清空、实际 `/yms save` 的事务历史备份和历史恢复，全部通过；历史恢复保留本服游戏模式与飞行能力。

最终 `/dmail claim` 中，Sync 真实 commit 成功后注入回执传输异常。Mail 最初得到未知结果；随后通过 Core 映射的只读保存证明自行恢复 CLAIMED，没有再次发物。最终增量严格为 3 个绿宝石；Sync 仍保留 FAILED 隔离，解隔离继续需要控制台核实，不由只读查询改变。邮件 ID `2`，交付 `probe_delivery_6bc1549c-4125-4a80-abe1-69a6b145fcab`，原领取操作 `9c189dbc-b044-8e16-85b5-b78853579aac`。恢复事件核对为唯一 CLAIMED 事件，保留原领取操作、会话和保存证明。

完整两台游戏服通过代理切换、真实 MOD 客户端界面和用户实际手持物品，仍由生产验收另外记录。数据库双节点竞争与单台真实 Youer 不能替代这些验证。
