# Saki 套餐确认与升级验收

2026-09-10，本机候选，尚未发布。工作区 `C:/DeiteriumAPP-IX/.worktrees/saki-upgrade-checkout`，分支 `feat/saki-upgrade-checkout`，基于已上线 2.0.6 的完整源码。

## 最终界面

遵循用户最后决定：使用圆角浮层、白色信息卡和蓝色“确认购买”按钮。App 与网页只显示最终付款金额及有效期，不显示计算公式、价差计算明细或天数取整说明。升级展示“到期时间不变”。聊天输入框上方的套餐入口仅出现在已加载且无任何消息的新会话；头像菜单保留。

## 验证结果

- Go 全量 `go test -race -tags integration ./... -count=1` 通过，包含原有商城、资金、Saki 与账号回归。新增升级用例覆盖购买时标价快照、金额精度、取整边界、原有效期、重复及并发、报价过期/改价/权益变化/账号隔离、旧客户端跨套餐拒绝、结算未知恢复、失败补偿与降级/退款拒绝。报价不跨越计费天数变化边界。
- OpenAPI 实际响应校验通过：普通购买、升级订单及报价共 6 个响应。新增报价复用既有报价表，无数据库迁移。
- Android `:ui-lab:assembleDebug :ui-lab:assembleDebugAndroidTest :ui-lab:testDebugUnitTest :ui-lab:lintDebug --offline --console=plain` 通过。
- 在 `emulator-5562` 覆盖安装后，原生仪器测试通过：取消不购买、开通成功停留套餐页、补款升级、禁用降级、浅深色与大字体、空会话入口显示/有消息隐藏、订单不可领取/退款及联系小祥。
- 仪器测试对比确认页与付款页的实际窗口 ID，证明转场共用同一窗口；同时验证动效开启、关闭两种状态。
- Web 60 项测试与生产构建通过。隔离数据库和模拟 Core 的真实浏览器流程通过普通购买、升级、到期时间不变、降级禁用、无公式；付款前后复用同一 dialog 元素。

界面与付款测试全部使用隔离测试账号、数据库和模拟经济节点；没有生产购买、开关调整或玩家数据变更。模拟器截图不等于真机手感验收。

## 本机交付

`C:/DeuteriumAPP/delivery/Saki-checkout-preview/` 包含 APK、最终原生截图与网页截图。APK 包名 `com.deuterium.app.uilab`，保留 `2.0.6 (20600)` 候选标识，未作为新版本发布；23,384,875 字节，SHA-256 `819ecd8766bf3ed07b9f0cf13918476f2dc4ba1616090d8579046c0385a24556`。新增购买报价及升级接口需要配套 Go 上线后才可在真实服务使用，当前生产 Saki 购买仍关闭。
