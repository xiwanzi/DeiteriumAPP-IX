# 通行申请与代理模块验收

2026-09-12。本次代码位于 `xiwanzi/admission-velocity`，基于已发布 App 2.0.13 / Web 2.0.15 整合主线，管理 Web 更新为 2.0.16。以下为本轮实际执行证据，不代表部署已经完成。

## 后端

- `go test -race -tags integration ./... -timeout 9m` 全部通过，HTTP 集成包运行约 438 秒；`go vet ./...` 通过。
- 新增 7 项数据库集成测试：公开提交校验/查询隔离/幂等重试、审批与撤销回执、并发审批仅一次、手动添加及取消旧断开指令、管理员 Cookie/CSRF、代理认证与数据库故障拒绝、导入预览及禁止复活撤销条目。
- 测试使用随机的回环 MariaDB 测试库，退出删除测试库，不读取生产 DSN。

## Velocity

- Java 21 构建和 10 项 JUnit 通过，产物 `Deuterium-Gateway-1.0.0.jar`。
- 覆盖持久记录及备份恢复、损坏不清空、自动回退不覆盖、手动切服更新、首次 Amiya/返回原服、无效响应拒绝、后端异常拒绝、旧连接迟到回调及尊重在线显式踢出。
- 使用游戏主机的 Velocity 4.1.1 / Java 25，在独立回环端口的隔离目录实际加载新插件；输出 ready、default=amiya，无类/方法链接错误，隔离进程正常停止。该探针未加载其他生产插件，使用无效专用测试凭据，不修改生产准入数据。

## Web 与独立申请站

- 管理网页构建通过；68 项现有 Node 测试通过。约 530KB 主 JS 包的大小提示保留，没有因此进行无关拆包。
- 使用真实本机 Go/MariaDB 联调：提交 → 待审回执 → 管理员通过 → 玩家查询可进入；移除、手动重新添加、历史记录与拒绝原因均通过实际浏览器操作。
- 人为让提交响应丢失，但允许服务端先完成：输入冻结并保留原请求，刷新后找回同一申请。修正了重新申请时过去撤销状态遮盖新待审状态的问题；新待审及拒绝结果显示正确。
- 公开页面 360、390、768、1024、1440 宽度检查；管理页 390 深色和 1440 浅色检查，无页面横向溢出。公约未默认勾选，阅读弹窗不自动同意。
- 浏览器夹具受环境变量控制、仅监听回环地址，测试登录入口不编入生产程序。生产完整游戏客户端切服行为仍应与本机/隔离验证区分。
