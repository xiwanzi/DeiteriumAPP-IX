# Android 2.0.0 接入与验收记录

本轮用户要求：保留现有 UI，仅修复圆形版本徽标开屏前的方形遮罩；移除模拟数据、演示账号和体验版专用工具；App 版本改为 2.0.0，接通实际后端与游戏服务。

## 构建与安装身份

- 模块 `android-app/ui-lab`，包名 `com.deuterium.app.uilab`，当前版本 `2.0.0 (20004)`，Android 8.0 / API 26 及以上，target SDK 35。下方早期构建段落保留为历史记录。
- 默认测试后端 `https://47.103.99.34`，可用 Gradle 属性 `deuteriumApiBase` 指定 HTTP(S) origin。使用系统 CA 与主机名/IP 校验；App 不包含服务器、S3 或游戏服凭据。
- 使用已有 debug 签名以覆盖 09 版。生产发布签名与正式渠道不在本轮测试安装包中变更。

## 本机已完成

- 开屏资源从 adaptive-icon 改为透明普通 drawable，系统不再为版本圆徽标套用自适应 App 图标轮廓；注册与关于页仍保留原 App 图标。
- 移除登录绕过、固定验证码、商城/联系人/钱包/公共聊天/委托/公告种子及模拟订单/管理员工具。升级不读取旧体验交易快照，外观设置保留。
- 账号使用真实登录、验证码、注册、改密与退出接口；会话令牌由 Android Keystore AES-GCM 加密保存。401 清理失效会话，网络失败不会视作成功。
- 公共聊天采用 Authorization WebSocket、服务端 ACK、历史拉取去重、重连；未确认消息保留原 clientMessageId。私聊、引用、人工转发、关注、公告、通知和资料已按 v2 接口实现，实服写入验收与实现范围分开记录。
- 钱包读取服务器余额/流水，未取得余额显示等待同步；转账先保存幂等请求，只有 success 才显示成功，processing/unknown 重启后继续核对。客户端不再执行本地扣款与自动结算。
- 更新检查只请求服务器清单，不再提供随包演示更新。

## 本次已执行的验证

- `JAVA_HOME=C:/Program Files/Zulu/zulu-17`，`ANDROID_HOME=C:/DeuteriumAPP/.tools/android-sdk`；命令 `./gradlew.bat :ui-lab:assembleDebug :ui-lab:testDebugUnitTest :ui-lab:lintDebug --console=plain` 首轮完成。
- 78 项单元测试通过，包括 WS 身份头、引用/幂等 ID 保留、拒绝不插入消息、断线结果未知，以及实时模式禁止本机资金状态机执行。
- Android 15 模拟器覆盖安装成功，旧本机登录态未继续进入；真实登录页没有体验入口。
- 对不存在的专用 QA 账号提交一次登录，经系统 TLS 校验到达后端，显示服务端“账号或密码错误。”。
- 通过独立 `androidTest` 仪器 APK 使用主任务签发的短期 xiwanzi QA 会话，实际通过 `/account/me` 身份核对、公共消息历史读取和带身份头的 WebSocket 握手。该会话不是密码登录验证，未修改玩家原密码；QA 令牌文件与仪器 APK 不进入最终交付包。
- 冷启动录屏及取样帧中徽标保持圆形，无方形底片。证据见同名目录 `android-v2-2026-09-08/`。
- 主任务部署 social/catalog/assets 增量后，Android 仪器实测目录、关注、私聊列表、公告、通知、偏好、官方商品、市场及本人发布、购物袋各接口均通过。产品/公告/市场空数组是实际服务器状态，未生成填充样例。
- 雨云 S3 实测通过：专用 16×16 PNG 创建上传授权、原始 bytes PUT、complete 达到 READY、通过资产授权 GET 解码并核对尺寸，然后调用 `/assets/{assetId}/remove`。未绑定/修改玩家头像；唯一 QA uploadId 为 `upload_0efecbb14636ed05603830ccd0eae3a0`，已软删除，已通知主任务做精确对象清理。证据 `live-social-catalog-assets.json`。
- 深色冷启动原始录屏与取样帧同样保持圆徽标；关闭灵动视效冷启动后进入真实已登录商城。验收后恢复模拟器浅色和开启动效。模拟器测试不替代实际手机体验。

## 2026-09-08 10:37 候选安装包

最新命令包含 `:ui-lab:assembleDebug :ui-lab:testDebugUnitTest :ui-lab:lintDebug :ui-lab:assembleDebugAndroidTest`，全部完成。78 项测试通过，0 failures / 0 errors / 0 skipped；Lint 0 errors / 24 warnings。APK 内已核验包含真实网络适配器，且不含旧示例商品图片和演示登录入口。

- 大小：31,279,685 bytes。
- SHA-256：`8ad1b8a96686da04fb9c32b0ab4e62bb8d6cb7470178df9d85b31b1f0ce24534`。
- 签名证书 SHA-256：`04a213066a78a968c2a1328a153a0967f99d117e0a826285e3f4af705db6f2f3`，与 09 版一致，已覆盖安装。
- 版本/权限信息经 APK 本身验证：`com.deuterium.app.uilab`、`2.0.0 (20000)`、minSdk 26、targetSdk 35、显示名 Deuterium。

这是供主任务继续资金联调的候选 APK，不代表 Minecraft 资金编排已完成，也未据此宣称全量上线。

## 仍在联调

后端增量与部署由主任务协调，本页不是全量上线报告。原密码登录成功、商城/市场/委托结算、Android 私聊/引用/转发实服写入和真机手感仍需继续验收。已经通过的资产、AI 与转账读取见上文及以下追加记录。当前不可用服务显示错误或空态，不用本机样例冒充可用。

## 2026-09-08 12:46 候选 20002

重新执行 `:ui-lab:assembleDebug :ui-lab:testDebugUnitTest :ui-lab:lintDebug :ui-lab:assembleDebugAndroidTest`，构建通过；82 tests / 0 failures / 0 errors / 0 skipped，Lint 0 errors / 25 warnings。修正候选快照遗漏：20001 APK 早于最后一次报价 Idempotency-Key 参数补丁，仪器对比发现 ABI 不匹配后提升 build code 重建，未把该问题归因于服务器。当前安装 APK 与仪器验证使用同一构建。

- `Deuterium-2.0.0-20002-test.apk`，22,667,625 bytes；SHA-256 `84999b11832f0ca47f232a0f2721ba48cfabacd4f8635dedcc96130e0c86df60`。
- APK 自身核验版本 2.0.0 (20002)、包名 com.deuterium.app.uilab、minSdk 26 / targetSdk 35，显示名 Deuterium；已成功覆盖安装。
- 报价为每次新购买意图保存 Idempotency-Key，同一次网络重试保持同键，购物袋/输入改变或付款流程结束后更新，避免再次购买复用已消耗报价。
- 真实 DeepSeek V4 Flash SSE 已在同一客户端实现完成一次调用：`d38ba8ae-c8cd-4435-a463-b411bac10bef`，回复 `aim_63e3df95f17ea23b25116a00e2336453`，实际搜索、380 字符和两条官方来源；同 ID 重放得到相同回复，平台管理员免额度。来源来自模型实际输出，origin 为 provider_text。证据 `live-ai.json` 和 `live-ai-instrumentation.txt`；本次升级未再次调用模型。
- 20002 独立仪器只读核对既有转账 `transfer_acad3a87845dcba74310068ae5ab734d791a7de1`：amount=1.00 / status=success / operationId=`coreop_258c2ad398e509840323eaf64e2883f9c686d1cc`，并通过余额/流水与收款人解析。未再次发起资金动作。
- 同次只读收到主任务已发送的公共消息 `msg_41ec45fa0ca4e1db827ab93ac9906f73`，账号与身份 WebSocket 也通过。没有重复发送消息。证据 `live-existing-transfer-public-chat-instrumentation.txt`、`live-existing-transfer.json`。
- 20002 UI 已恢复同一 AI 历史，配额入口显示“管理员 · AI 次数不限”，套餐面板显示 ProMax 免费 20 次 / 24 小时、Pro 与 AI Ultra 均为 9999999.00 信用点 / 暂未开放。没有点击新对话或再次生成回答；截图 `v20002-ai-history.png`、`v20002-ai-plans.png`，已目视检查。

## 2026-09-08 13:05 当前候选 20003

补齐请求未到达服务器的恢复边界：只有本地原请求没有 operationId，且按原 clientRequestId 查询得到结构化 NOT_FOUND/404，才向原端点重放原请求。已登记 operationId 即使查询 404 也不重放；网络、UNKNOWN、非结构化代理 404 均保留待核对。原报价 QUOTE_EXPIRED 明确结束该意图，让用户重新确认，不能自动替换 quoteId、金额或 key。

原请求持久化绑定 owner+origin，并在查询和重放前后核对当前身份。期间切账号/换服务器不会用新身份付款或把旧回执记入新账号；请求 Bearer 在切换 IO 线程前固定。会话本身也绑定 origin，旧候选没有记录该字段的会话在升级后失效一次，重新认证后正常保存。原记录缺少 scope 时保留并提示核对，不自动重放。

- 完整构建、单元测试、Lint、仪器 APK 构建通过，91 tests / 0 failures / 0 errors / 0 skipped；Lint 0 errors / 25 warnings。新增 9 项恢复测试覆盖三个原端点、未知/失败保持、报价过期、缺失 origin、跨账号/跨服务器和请求中途身份切换。
- APK `Deuterium-2.0.0-20003-test.apk`，22,684,009 bytes，SHA-256 `894b523b83aa2c9e2f9f2f7b8cec6ca601f3d00ac00572b30cd258c5cda556ad`。版本和包名经 APK 本身核验，签名仍为 `04a213066a78a968c2a1328a153a0967f99d117e0a826285e3f4af705db6f2f3`，覆盖安装成功。
- 新短期 QA 会话下，实际 20003 再次读取同一既有 1.00 成功转账、原 Core operationId、原公共消息、余额/流水和身份 WebSocket，全部通过。无资金、聊天或 AI 新增调用；证据 `v20003-live-existing-transfer-public-chat.txt`。

## 2026-09-08 13:46 最终服务部署后的列表只读联测

主任务确认 Go `00fbdac` 与 013/017 部署后，继续使用实际安装的 20003 APK。只重新构建独立 androidTest 仪器，没有重建或替换 App 安装包；安装路径 base.apk 的 SHA-256 与交付 20003 文件逐字节一致，仍为 `894b523b83aa2c9e2f9f2f7b8cec6ca601f3d00ac00572b30cd258c5cda556ad`。

安装包自身 `BackendApi.request` 和 `BackendApi.listAll` 对以下端点均成功读取并解析：`/orders`、`/commissions`、`/commissions/me`、`/store/products`、`/market/listings`、`/market/me/listings`。六个服务端数组当前各为 0 条；六个分页均为 `hasMore=false / nextCursor=null`。这些是实际 HTTPS 响应，未遇到原先未部署时的 404，没有注入或回填本机样例。

同次购物袋/catalog、账号、余额/流水与收款人解析、既有 `transfer_acad3a87845dcba74310068ae5ab734d791a7de1` 的 1.00 成功回执、既有公共消息 `msg_41ec45fa0ca4e1db827ab93ac9906f73`、认证 WebSocket 握手全部通过。所有业务请求均为 GET，没有付款、发消息、调用 AI 或上传资产。短期会话输入文件已由仪器删除。

证据：`v20003-final-financial-lists.json` 保存逐端点条数、真实分页对象和安装 APK 哈希；`v20003-final-financial-lists-instrumentation.txt` 保存仪器 PASS 结果。此结果证明金融列表已经可读并正确解析，不替代非空订单状态或实服资金写入验收。

## 20004：真实图片与原转账恢复修复

他人商品的通用 `/assets/{id}` 查询实际返回 404，但业务响应已经给出合法签名图片 URL。此前客户端忽略该 URL，图片失败后显示默认资源；HTTPS 头像也被当作本地 Uri。现按原账号/origin缓存内嵌 AssetView URL及到期时间，保留assetId供编辑提交；列表/资料刷新替换缓存和渲染键。过期链接要求刷新业务页面，不只重试owner-only资产接口。对象图片 GET 使用独立无Bearer客户端。

转账每2.5秒只查询已知原transferId，确认成功立即清pending并解锁。付款完成状态绑定当前clientRequestId，不能把上一笔成功当成下一笔。收款人搜索保留已有头像，选中后读取完整资料。后端对原Core回执字段的修复由主任务单独部署，客户端未重付款。

- 12项定向测试通过：RemoteImageUrlsTest与PendingOperationRecoveryTest。此次按要求没有重跑其他业务测试或Lint。
- Android15模拟器真实读取并解码商品照片512×288、ElmastorVox资料头像640×640；原 `transfer_732103cd41d23562ef51c6ebae75edfde4995431` 恢复success，原 `coreop_0b2834feb9000263f3e29c84f4d96c13bff272da` 不变，pending清除、新转账解锁，业务写入0次。
- 证据：`v20004-user-fixes.json`、`v20004-user-fixes-instrumentation.txt`，不含签名URL或会话密钥。
- 包含主任务UI提交 `c89f4d4`、`93a800a`；APK 2.0.0(20004)，22,684,005 bytes，SHA-256 `bb48ece21019d8ba25e554e2e6b13f94876545bf9019de5874afc127a2c5f7a3`，原签名证书不变。构建、APK身份/签名校验、覆盖安装均通过。
