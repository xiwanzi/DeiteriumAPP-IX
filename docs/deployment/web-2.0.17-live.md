# Web 2.0.17 白名单切换修复与全员资格重置

2026-09-13 **00:10:49（UTC+8）**，Web **2.0.17** 上线。[后台白名单管理](https://47.103.99.34/admin?section=whitelist) 切回申请列表导致整页白屏的问题已修复。代码为 [PR #31](https://github.com/xiwanzi/DeiteriumAPP-IX/pull/31)，构建提交 `0778123edd89228ff0231f2e194e733d85ce5d21`，合并提交 `906bbae5b5c21c153b0b4c78ee0dd77cb5521521`。

响应绑定视图及完整查询条件，切换后的首帧不会把白名单记录当成申请记录渲染。保留旧请求代次保护，延迟返回的请求不能覆盖当前结果。

## 验证与部署

- 原版 Chromium 复现 `interests.join` 异常及 React 根节点清空。
- 修复后，隔离数据验证空/非空申请、快速切换共 30 次，以及摘要入口、筛选、详情、响应乱序；没有未捕获异常。
- Web 68 项测试及生产构建通过。构建仅提示已有的包体积建议。
- 公网实际 HTML、JS、CSS 与构建哈希一致，版本显示 2.0.17。额外使用公网正式产物和浏览器内隔离 API 响应，完整管理页往返切换 10 次通过；此项没有模拟成真实管理员会话，也没有操作生产玩家。
- 通过原子切换静态目录发布，无需重启服务。后端进程、后端配置、Nginx 配置及独立申请站路径均核对一致。App 2.0.13、Go 与 Gateway 1.0.0 沿用原版本。

产物 ZIP SHA-256：`8959c048550a2e75f23b67408b0e4044011f90bd627ff39287baa2191e4ae558`。当前目录 `/var/www/deuterium/releases/web-2.0.17-0778123edd89/dist`；前版 `/var/www/deuterium/releases/web-2.0.16-990a3c9eac3d/dist` 保留。新目录兼容保留前版哈希 JS，供切换瞬间已读取旧 HTML 的请求使用。

网页备份 `/var/backups/deuterium/web-v217-0778123edd89`。仅网页回退可原子切回前版目录；前版含已知白屏问题，优先前向修复。回退网页不应恢复任何业务数据库。

## 用户明确要求：所有玩家重新申请

用户要求：“顺带把玩家的所有白名单移除了，这是一个新的开始。”在 **00:14:02（UTC+8）**，通过现有管理员撤销接口完成 **70 个当前有效玩家资格**的逐条撤销，包含原先导入的旧账号资格，没有豁免管理员自己的游戏准入资格。操作后 **有效白名单为 0，已移除为 70**；每条操作保留版本更新、幂等请求及审计记录。

账号、既有订单/委托、申请及操作历史保留，没有删除玩家数据。撤销使用 `kickOnline=false`，没有主动断开当时 1 位在线玩家；后续新登录必须重新取得资格。逐一向代理使用的 `/bridge/v1/admission/check` 查询这 70 个 UUID，全部返回 `REVOKED / allowed=false`。临时运维会话已撤销并验证 401；没有新增待执行踢出指令。

后续申请继续正常审批。**之前“保留 70 个旧玩家资格”的上线记录已被本次明确指令覆盖，不可自动恢复旧资格或把“已移除”记录清空后重新导入。** 原有重连子服记录与默认 Amiya 不变。

重置前名单、相关表及执行记录保存在 `/var/backups/deuterium/whitelist-reset-20260912T161358Z`，备份 SHA-256：`820febfb81be4a6800961f7f38c9df866c10cb452ed0e072e0d7d638ed5fe41e`。该备份含个人信息，仅保存在受限运维目录，不进入仓库。名单恢复需以后新的明确授权，不能作为常规部署回退的一部分。

[回归记录](../qa/whitelist-tab-crash.md) · [发布及重置验证](artifacts/web-v217/verification.json) · [原准入模块部署](admission-v1-live.md)
