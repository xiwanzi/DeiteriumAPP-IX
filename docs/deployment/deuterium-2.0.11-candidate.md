# App 2.0.11 发布候选

2026-09-11，用户已授权合并远端并推送 App 更新。版本递增为 **2.0.11（21100）**，更新说明原文为：

> 大幅提升流畅度，提升系统稳定性。

本次包含 `0492730` 的全 App 性能优化与 `81996cc` 的全局过度绘制修正，沿用当前签名、包名和非 debuggable 的 `performance` 构建。原素材、玻璃、动画和业务契约保持当前方案，详见[性能验收](../qa/app-performance-equivalence-v210.md)、[过度绘制真机验收](../qa/app-global-overdraw-v210.md)。这些证据来自本轮此前已完成的优化验证，不冒充版本递增后的新测试。

发布前将完成新版本构建、单元测试、APK 元数据/签名检查和公开下载摘要核对。APK 上传至既有 `/downloads/` 渠道，备份并原子更新发布清单，重启现有 Go 服务加载清单后检查健康、旧版更新与新版无重复更新。Go/Web/游戏插件文件及数据库结构不在此次发布改动范围内。

更新检测面向现有客户端，额外账号提醒沿用此前发布流程，发送至 `xiwanzi`，使用唯一事件键 `app.release:21100`，正文采用上述原文，不广播公共聊天。

当前文件记录发布准备；线上完成状态以后续部署记录和实际公开接口核验为准。
